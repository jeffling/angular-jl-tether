{
  "name": "ng-tether",
  "version": "0.1.0",
  "description": "Tether wrapper for Angular, so you can use it in directives declaratively",
  "main": "ng-tether.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deps": "npm install && bower install",
    "build": "webpack --config webpack.config.js ng-tether.coffee ng-tether.js",
    "build-min": "webpack --optimize-minimize --config webpack.config.js ng-tether.coffee ng-tether.min.js",
    "dist": "npm run build & npm run build-min",
    "watch": "webpack --config webpack.config.js --watch ng-tether.coffee ng-tether.js"
  },
  "author": "Jeff Ling <jeff@bench.co>",
  "license": "MIT",
  "devDependencies": {
    "webpack": "^1.1.10",
    "coffee-loader": "^0.7.1",
    "bower": "^1.3.3",
    "exports-loader": "^0.6.2"
  }
}
